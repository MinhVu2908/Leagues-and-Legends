{
  "name": "leagues-and-legends",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd arena && vite",
    "build": "cd arena && vite build --outDir dist",
    "preview": "cd arena && vite preview --port 5173"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
